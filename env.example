# Frontend Configuration
VITE_API_URL=http://localhost:3001/api

# Инструкции по настройке:
# 1. Скопируйте этот файл в .env.local
# 2. Настройте URL бэкенда (по умолчанию http://localhost:3001/api)
# 3. Не коммитьте .env.local в git (он уже в .gitignore)

# API ключи YandexGPT теперь настраиваются на бэкенде
# См. backend/.env для настройки YANDEX_API_KEY и YANDEX_FOLDER_ID